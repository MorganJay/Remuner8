import React from 'react';

const CustomSelect = ({ options }) => {

  return (
        <select>
            <option value="AL">
                Alabama
            </option>
        </select>
  );
};

export default CustomSelect;